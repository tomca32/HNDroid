<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
    </style>

    <style name="storyItemContainer">
        <item name="android:paddingTop">@dimen/standardPadding</item>
        <item name="android:paddingBottom">@dimen/standardPadding</item>
        <item name="android:clickable">true</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
    </style>
    <style name="storyTitleText">
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>
    <style name="storyUrlText">
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/colorTextSecondary</item>
    </style>
    <style name="storyCommentContainer">
    </style>
    <style name="storyNumberText">
        <item name="android:paddingLeft">@dimen/standardPadding</item>
        <item name="android:paddingRight">@dimen/standardPadding</item>
        <item name="android:textAlignment">center</item>
    </style>
</resources>
